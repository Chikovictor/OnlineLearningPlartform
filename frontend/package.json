{
  "name": "online-learning-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.0.0",  // Axios: Library for making HTTP requests to backend.
    "bootstrap": "^5.0.0",  // Bootstrap 5: For styling and responsive UI.
    "react": "^18.0.0",  // React 18+: Core library for building UI.
    "react-dom": "^18.0.0",  // React DOM: For rendering to browser.
    "react-scripts": "5.0.1"  // Scripts for build/run.
  },
  "scripts": {
    "start": "react-scripts start",  // Starts dev server at localhost:3000.
    "build": "react-scripts build"  // Builds for production.
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}